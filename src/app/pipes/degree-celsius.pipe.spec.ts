import { DegreeCelsiusPipe } from './degree-celsius.pipe';

describe('DegreeCelsiusPipe', () => {
  it('create an instance', () => {
    const pipe = new DegreeCelsiusPipe();
    expect(pipe).toBeTruthy();
  });
});
